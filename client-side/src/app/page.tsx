"use client";

import { ProductCard, Slider, ProductsList } from "@/components";

const Home = () => {
  return (
    <div className="m-5">
      <ProductsList />
    </div>
  );
};

export default Home;
